language: none

infra:
  pre_prov:
    - terraform plan

  prov:
    - terraform apply -state=/shippableci/shippable/provision/terraform.tfstate
    - sleep 60

  # post_prov:
  #   - yes yes 2>/dev/null | terraform destroy

integrations:
  notifications:
    - integrationName: email
      type: email
      recipients:
        - ankul@shippable.com
      on_success: always
      on_failure: always
      on_start: always
